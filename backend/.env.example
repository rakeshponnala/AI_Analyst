# =============================================================================
# HEDGEAI BACKEND CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your values:
# cp .env.example .env

# -----------------------------------------------------------------------------
# Anthropic API Configuration
# -----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/
# REQUIRED: Application will not start without this
ANTHROPIC_API_KEY=your_api_key_here

# AI model to use (do not change unless you know what you're doing)
# Available models: claude-sonnet-4-5-20250929, claude-3-5-sonnet-20241022
AI_MODEL_NAME=claude-sonnet-4-5-20250929

# Maximum tokens in AI response (150-2000 recommended)
# Higher = more detailed analysis but slower and more expensive
AI_MAX_TOKENS=1500

# AI temperature (0.0-1.0)
# Lower = more consistent/factual, Higher = more creative
# Recommended: 0.2-0.3 for financial analysis
AI_TEMPERATURE=0.3

# -----------------------------------------------------------------------------
# Data Source Configuration
# -----------------------------------------------------------------------------
# Number of news headlines to fetch (1-10)
# More news = better context but slower response
NEWS_MAX_RESULTS=5

# -----------------------------------------------------------------------------
# API Timeout Configuration (in seconds)
# -----------------------------------------------------------------------------
# Timeout for external data APIs (Yahoo Finance, DuckDuckGo)
EXTERNAL_API_TIMEOUT=30

# Timeout for Anthropic API calls
ANTHROPIC_API_TIMEOUT=60

# -----------------------------------------------------------------------------
# Cache Configuration (TTL in seconds)
# -----------------------------------------------------------------------------
# How long to cache stock data (default: 300 = 5 minutes)
STOCK_DATA_CACHE_TTL=300

# How long to cache news data (default: 900 = 15 minutes)
NEWS_CACHE_TTL=900

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# Development: http://localhost:3000,http://127.0.0.1:3000
# Production: https://yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
